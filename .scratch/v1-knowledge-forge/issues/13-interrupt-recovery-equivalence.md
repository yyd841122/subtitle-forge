# 13 — 中断恢复等价

**What to build（单一产品增量）:** 运行被人为中断后再次运行：已完成 Source 复用（不重复处理），未完成继续执行至完成；最终**全部规范资产**（忠实层、快照、全局产物；后续票新增的规范内容形态自然纳入等价范围）与无中断的完整运行逐内容等价（忽略运行元数据）。

**Spec anchors:** A19、R5.3。

**Blocked by（硬依赖）:** 11。软依赖/顺序建议：12 先行，非硬。

**Status:** open（2026-09-02 materialized）

**Materialized from:** plan v2.2 票 12（ADR-0007）

**Acceptance（端到端断言，只对外部产物）:**
- [ ] 子进程杀掉第二个 Source 处理中的运行 → 重跑 → 首个 Source 表现为 skipped/引用既有产出
- [ ] 被杀 Source 的残留不完整快照不被当作有效（联动 10 的完整性标记）
- [ ] 全量完成后与一次性完整运行做**全部规范资产的内容级对比**（不止 trusted/gap/faithful 子集）

**Implementation / code anchors:** 10 + 11 的组合（本票主要是中断场景的等价性补齐与验收——全局产物在中断后重建的正确性、残留状态判定）。

**明确不含（触界行为）:** Source 内部更细粒度恢复（Source 粒度已满足 A19；更细属 16 之后的选择）。

**票内裁定:** 中断检测与残留状态处理的初始策略。

**Spec 覆盖责任:** A19（完整）；R5.3。
