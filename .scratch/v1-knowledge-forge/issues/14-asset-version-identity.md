# 14 — 资产版本标识与同版本语义显式化

**What to build（单一产品增量）:** 资产版本成为运行请求与产物的一等可观察标识：以显式版本运行全新资产目录 → 全部产物与快照自描述该版本；同版本重跑沿用 11/12 幂等语义（版本因子从平凡真变为真实比对）。

**Spec anchors:** R5.4（区分资产版本）、A5（同版本语义的显式形态）、A20（前半：同版本内无新旧混杂——11 已保证，本票在显式版本下复验）。

**Blocked by（硬依赖）:** 11。

**Status:** open（2026-09-02 materialized）

**Materialized from:** plan v2.2 票 13a（ADR-0007）

**Acceptance（端到端断言，只对外部产物）:**
- [ ] 以版本 v1 运行 → 产物/快照自描述 v1
- [ ] 同 v1 重跑 → 11 幂等断言原样成立
- [ ] 对已有 v1 资产目录请求 v2 → **明确拒绝**（15 落切换，本票触界 fail loud，不产出半切换状态）

**Implementation / code anchors:** 10 票快照的版本占位、11 四要素中的版本因子。

**明确不含（触界行为）:** 对已有资产目录的版本切换、旧版本保留/共存（15）。

**票内裁定:** Open Impl 3（版本字段形态、递增方式、存放位置）。

**Spec 覆盖责任:** A20（同版本部分）、A5（显式版本复验）；R5.4。
