# 21 — 可信与覆盖指标成对（确定性基准）

**What to build（单一产品增量）:** 每次完整运行的摘要同时包含可信性指标与覆盖性指标；保守提炼（极少单元）在高通过率下，覆盖指标确定性地反映低覆盖。

**Spec anchors:** R4.5、R4.1（指标以知识覆盖为尺度——非文本覆盖）、A16、ADR-0003。

**Blocked by（硬依赖）:** 19、20。

**Status:** open（2026-09-02 materialized）

**Materialized from:** plan v2.2 票 18（ADR-0007）

**Acceptance（端到端断言，只对外部产物）:**
- [ ] 指标定义先行（票内裁定后落断言）：可信性指标 = 单元级通过率，**分母 = 当前版本全部候选知识单元**（passed+rejected+needs_review+failed；聚合知识一律排除——19 为硬依赖，此断言无条件执行）；覆盖性指标 = 覆盖审计结论的量化初始形态；空 Source、failed Source 的取值规则票内裁定并落断言
- [ ] 正常运行 → 两类指标并存（只报其一即失败）
- [ ] **保守 fixture 钉死**：提炼替身只产 1 单元（全通过）+ 覆盖审计替身对该 Source **预设低覆盖量值/等级** → 高通过率与低覆盖**并排可见**，断言覆盖指标不因通过率高而抬高

**Implementation / code anchors:** `RunSummary` 结构扩展；指标计算在管线尾部；`CoverageVerdict` 需扩展量化形态（字段形态票内裁定）。

**明确不含（触界行为）:** 指标阈值/目标（真实数据校准，PRODUCT 明示不预拍）。

**票内裁定:** 覆盖量值/等级的初始形态与分母规则。

**Spec 覆盖责任:** A16、A18（分母排除断言）；R4.5、R4.1。
