# 23 — 受限运行不降门槛（最小策略钉死）

**What to build（单一产品增量）:** 受限审计配置下运行：被审单元的准入结论与全量配置一致；未审单元走 needs_review/缺口而非直接通过。

**Spec anchors:** R7.2、A22。

**Blocked by（硬依赖）:** 04、05、22。

**Status:** open（2026-09-02 materialized）

**Materialized from:** plan v2.2 票 20（ADR-0007）

**Acceptance（端到端断言，只对外部产物）:** 构造高审计成本输入，全量 vs 受限两配置运行 →
- [ ] 被审单元准入结论一致（受限运行走**同一准入门**：程序比对 + 推理审计，04 为硬依赖）
- [ ] 受限下未审单元不进发布集（needs_review 且理由「受限运行未审」，是否加缺口条目票内裁定）
- [ ] 计量记录显示受限运行成本更低（22 联动，确定性断言）

**Implementation / code anchors:** 运行请求配置（新增受限形态）；管线审计调度按配置分级。

**明确不含（触界行为）:** 策略调优与完整体系（PRODUCT：后续验证的实现决策）。

**票内裁定:** **最小策略直接钉死**：`max_audited_units = N`，按稳定顺序（如 unit_id 序）取前 N（N 与顺序键票内确认）；其余为 Open Impl 9 的初始值记录。

**Spec 覆盖责任:** A22；R7.2、R3.8（降级触发 needs_review 的用法）。
