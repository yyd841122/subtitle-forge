# 25 — 规范源格式终裁（含迁移如需）

**What to build（单一产品增量）:** 对规范源格式作最终裁定（维持「人读 Markdown + 内嵌 JSON」或迁移），校验对象 = 届时全部内容形态（快照、忠实层、注记、聚合、版本标识、指标、衍生层）。

**Spec anchors:** R6.1/R6.3/R6.6、ADR-0005/0006（终裁承诺）。

**Blocked by（硬依赖）:** 10、15、18、19、21、22、24（全部内容形态就位后再终裁，均硬）。

**Status:** open（2026-09-02 materialized）

**Materialized from:** plan v2.2 票 22a（ADR-0007）

**Acceptance（端到端断言，只对外部产物）:**
- [ ] 终裁结论记录 ADR（Open Impl 7 关闭）
- [ ] 若维持：全部内容形态在现有格式下的双读断言补齐
- [ ] 若迁移：一次性完成迁移；**测试更新规则先声明**——现有测试中硬编码的文件名（如 `trusted-set.md`）允许改为新文件名，但断言语义（内容与结构）不变；文件发现一律改为从自描述清单（`asset_organization` 或其继任者）读取，为 26 黑盒发现铺路
- [ ] 迁移后既有端到端接缝全绿

**Implementation / code anchors:** `assets.py` 全部写读函数；10 票 reader（若迁移，reader 是集中改动点）。

**明确不含（触界行为）:** 格式迁移的自动化通用工具（一次性脚本即可）；多格式引擎预留；A8 空环境黑盒验收（26）。

**票内裁定:** Open Impl 7 终裁；迁移路径（如需）。

**Spec 覆盖责任:** R6.1/R6.3/R6.6（格式侧；可移植性行为验收在 26）。
