# 26 — 资产可移植性黑盒验收（A8）

**What to build（单一产品增量）:** 以**不依赖本系统**的黑盒消费者验收资产可移植性：复制到空环境后人可直接阅读、程序可解析结构、删除派生层可重建且知识无损。

**Spec anchors:** A8、R6.1/R6.3。

**Blocked by（硬依赖）:** 24、25。

**Status:** open（2026-09-02 materialized）

**Materialized from:** plan v2.2 票 22b（ADR-0007）

**Acceptance（端到端断言，只对外部产物）:** 测试**不 import `subtitle_forge`**（重建入口以外部进程运行 CLI）：
- [ ] 资产目录复制到空环境 → 依自描述清单发现文件 → 人可读断言 + 通用 JSON 解析提取知识单元/来源引用/实体状态
- [ ] 删除派生索引缓存 → 外部重建 → 知识无损（与删除前内容等价）

**Implementation / code anchors:** 25 之后的格式；24 的重建入口。

**明确不含（触界行为）:** 规范源的在线服务化/检索系统（Out of Scope）。

**票内裁定:** 黑盒发现的清单格式（25 已铺路，本票确认）。

**Spec 覆盖责任:** A8（完整）；R6.1/R6.3（黑盒复验）。
